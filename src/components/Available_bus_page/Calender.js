import React from "react";
import data from "../../Data.json"

function Calender() {
  return (
  <div className="my-5 ">
    {/* {
        data.map((data)=>{
        return(
          <div>
          {data.cities.map((bus)=>{
            return(
              <div>{bus}</div>
            )
          })}
          </div>
      )
      })
    } */}
  </div>
  );
}

export default Calender;
